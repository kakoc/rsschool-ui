// import { ITask } from '../models';

// const getTasksNames = (courses: Array<ICourse>) => {
//     return courses.map((course: ICourse) => ({
//         _id: course._id,
//         name: course.name,
//     }));
// };

// export { getCoursesNames };
